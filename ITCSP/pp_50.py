L1 = [
    ("Bread", 10),
    ("Butter", 20),
    ("Chocolate dark", 15),
    ("Chocolate white", 17),
    ("Cakes", 19)
]

# mine
# def get_price_filtered_list(filtering_critetion: int, items: list[tuple[str, int]]) -> list[tuple[str, int]]:


#     price_filtered = list(map(lambda item: item[1], L1))
#     print(price_filtered)

# Tifas
# new_list = [x for x in items if x[1] >= filtering_critetion]
# return new_list

# print(get_price_filtered_list(15, L1))

# teachers

# def
#     price_filtered = []
#     for item in items:
#         if item[1] > filtering_critetion:
#             price_filtered.append(item)
#     return get_price_filtered_list

#     print(get_price_filtered_list(filtering_critetion=15, items=L1))
